# Server
NODE_ENV=development
PORT=4000
APP_URL=http://localhost:4000
CORS_ORIGINS=http://localhost:5173
TRUST_PROXY=0

# Required in production (generate with: openssl rand -base64 48)
JWT_SECRET=replace-with-at-least-32-random-characters
JWT_EXPIRES_IN=8h
ADMIN_NAME=WORLDGPZ Administrator
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=replace-with-a-unique-strong-password

# Render PostgreSQL. Leave blank locally to use server/data/worldgpz.json.
DATABASE_URL=
DATABASE_SSL=false

# Optional data providers. Keys stay server-side and are never sent to the browser.
NEWS_API_KEY=
OPENWEATHER_API_KEY=

# Optional OpenAI-compatible intelligence provider.
AI_API_KEY=
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini

# Cache/live-source tuning
SOURCE_CACHE_SECONDS=300
FETCH_TIMEOUT_MS=8000
